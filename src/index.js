module.exports = function check(str, bracketsConfig) {
  // your solution
  
		  

  
	    for (var i=0; i<bracketsConfig.length; i++)
		for(var j=0;j<bracketsConfig.length;j++){
  var subAr=bracketsConfig[i][j];
  }
  var str='';
  var el=str.split('');
  
    	
	  if(el.length%2!=0 && el!=subAr){
	  return 0;
	  }else{
	  return 1;}
	 
	  
  
	  
  
}
  
	  
  

